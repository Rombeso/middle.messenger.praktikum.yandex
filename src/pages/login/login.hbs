{{!< main}}
<main class="main">
    <form class="loginForm" action="/">
        <div class="loginForm__block">
        <h3>Sign in</h3>
            {{#each logInput}}
                {{> 'input/input' this}}
            {{/each}}
        </div>
        <div class="loginForm__bottom">
        {{> 'button/button'  button-title="Sign In" button-class='button'}}
            <a class="loginForm__link" href="../signUp/singUp.hbs">Create account</a>
        </div>
    </form>
        <a href="../index/index.hbs" class="backLink">Вернуться в меню</a>
</main>
